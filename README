Verze Javy potřebná ke spuštění (resp. verze na které to určitě běží)
výpis `java -version`

java version "1.6.0_20"
OpenJDK Runtime Environment (IcedTea6 1.9.7) (suse-1.2.1-x86_64)
OpenJDK 64-Bit Server VM (build 19.0-b09, mixed mode)


Použitelné cíle pro ant:
    compile - zkompiluje do .class souborů
    jar - vytvoří spustitelný IWReview.jar
    run - spustí program
    sample - spustí program s přiloženou mapou
    javadoc - vygeneruje javadoc dokumentaci
    clean - vyčistí soubory, které lze vygenerovat
